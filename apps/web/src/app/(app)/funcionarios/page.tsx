import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function FuncionariosPage() {
  return (
    <PlaceholderPage
      titulo="Funcionários"
      descricao="Cadastro de usuários, papéis e permissões (ADMIN) — doc 09."
    />
  );
}
