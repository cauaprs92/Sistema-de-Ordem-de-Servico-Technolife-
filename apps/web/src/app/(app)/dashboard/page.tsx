export default function DashboardPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-muted-foreground">
        Indicadores por papel (doc 00) — implementado na Sprint 7.
      </p>
    </main>
  );
}
