import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function DashboardPage() {
  return (
    <PlaceholderPage
      titulo="Dashboard"
      descricao="Indicadores, gráficos e atalhos por papel (doc 00) — implementado na Sprint 7."
    />
  );
}
