import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function EstoquePage() {
  return (
    <PlaceholderPage
      titulo="Estoque"
      descricao="Produtos, entrada de compra, kardex e alertas de mínimo — Sprint 2 (doc 04)."
    />
  );
}
